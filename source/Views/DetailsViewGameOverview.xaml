<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    mc:Ignorable="d">

    <Style TargetType="{x:Type DetailsViewGameOverview}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DetailsViewGameOverview}">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto"
                                  Style="{DynamicResource DetailsScrollViewer}"
                                  d:DesignWidth="1500"
                                  d:DesignHeight="1700"
                                  d:DataContext="{x:Static DesignMainViewModel.DesignSelectedGameDetailsIntance}"
                                  x:Name="PART_ScrollViewHost">
                        <Grid Margin="0,0,0,0" x:Name="MainGrid">
                            <Grid x:Name="BackgroundImageGrid" Height="{Binding ElementName=TopHeader, Path=ActualHeight}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Top">
                                <Viewbox Stretch="UniformToFill" >
                                    <FadeImage Name="BlurGameBackground" ImageDarkeningBrush="#990E0E0E"
                                               HighQualityBlur="True" SourceUpdateDelay="100"
                                               IsBlurEnabled="True" AnimationEnabled="{Settings BackgroundImageAnimation}"
                                               Tag="{DynamicResource DetailsViewBlurGameBackgroundImage}"
                                               Height="96" RenderOptions.BitmapScalingMode="HighQuality" BlurAmount="3"
                                               HorizontalAlignment="Center" VerticalAlignment="Top">
                                        <FadeImage.Style>
                                            <Style TargetType="FadeImage">
                                                <Setter Property="Source" Value="{Binding ElementName=PART_ImageBackground, Path=Source}" />
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=PART_ImageBackground, Path=Source}" Value="{x:Null}">
                                                        <Setter Property="Source" Value="{x:Null}" />
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
                                                        <Setter Property="Source" Value="{x:Null}" />
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </FadeImage.Style>
                                    </FadeImage>
                                </Viewbox>

                                <Grid>
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=BlurGameBackground, Path=Visibility}" Value="Visible">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>

                                    <Border x:Name="PART_ImageBackground_Wrapper">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=HasDataBackground, FallbackValue=False}" Value="True" />
                                                            <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=EnableBackgroundImage, FallbackValue=False}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <FadeImage x:Name="PART_ImageBackground" ClipToBounds="True"
                                                   Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-200,0,0" />
                                    </Border>

                                    <ContentControl x:Name="BackgroundChanger_PluginBackgroundImage"
                                                ClipToBounds="True"
                                                HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0" />

                                    <Border Background="#77000000" />

                                    <Border Background="{DynamicResource DarkNoiseBrush}"
                                            Opacity="0.3"/>
                                </Grid>

                                <Border Visibility="{Binding ElementName=PART_ImageBackground, Path=Source, Converter={StaticResource NullToVisibilityConverter}}"
                                        Background="{DynamicResource NoiseBrush}" Opacity="{DynamicResource BackgroundImageNoiseBackgroundOpacity}"/>
                            </Grid>

                            <Grid HorizontalAlignment="Stretch">
                                <Grid.Resources>
                                    <Style TargetType="Border" x:Key="DetailsPanelRoundedBorder">
                                        <Setter Property="CornerRadius" Value="3" />
                                        <Setter Property="Background" Value="{DynamicResource DetailsViewPanelBackgroundBrush}" />
                                        <Setter Property="Padding" Value="20,14,20,14" />
                                        <Setter Property="Margin" Value="0,0,0,32" />
                                    </Style>
                                </Grid.Resources>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Name="TopHeader">
                                    <Border Visibility="Hidden" Height="{Settings CalculatedGameDetailsIndentation}"
                                            MinHeight="{DynamicResource PanelTopPanelSeparatorHeight}" />
                                    <Grid Margin="40,0,40,0" MaxWidth="{DynamicResource DetailsViewDetailsMaxWidth}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="5*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="4*" />
                                        </Grid.ColumnDefinitions>

                                        <Grid Grid.Column="0" MinHeight="400">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <WrapPanel Grid.Row="0">
                                                <DockPanel Width="Auto">
                                                    <Image Name="PART_ImageIcon" MaxHeight="48" MaxWidth="48" DockPanel.Dock="Left" 
                                                           Margin="0,10,10,0" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="Fant">
                                                        <Image.Effect>
                                                            <DropShadowEffect BlurRadius="0" ShadowDepth="10" Color="#0d0d0d" Opacity="0.90"/>
                                                        </Image.Effect>
                                                        <Image.Clip>
                                                            <RectangleGeometry RadiusX="1" RadiusY="1" Rect="0,0,48,48" />
                                                        </Image.Clip>
                                                    </Image>

                                                    <ContentControl x:Name="DuplicateHider_SourceSelector" 
                                                            DockPanel.Dock="Left" VerticalAlignment="Top"
                                                            VerticalContentAlignment="Center"   
                                                            Margin="0,5,5,0"  
                                                            Height="24"
                                                            Visibility="{PluginSettings Plugin=DuplicateHider, Path=EnableUiIntegration, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                            RenderOptions.BitmapScalingMode="Fant" />

                                                    <Border DockPanel.Dock="Left"
                                                            Tag="{DynamicResource DetailsViewDisplayGameNameIfLogoVisible}" Width="Auto" ClipToBounds="False">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}, FallbackValue=False}" Value="False" />
                                                                            <Condition Binding="{Binding ElementName=ExtraMetadataLoader_LogoLoaderControl, Path=Visibility, FallbackValue=Collapsed}" Value="Visible" />
                                                                            <Condition Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=IsLogoAvailable, FallbackValue=False}" Value="True" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock FontFamily="ZELDA" Typography.Capitals="SmallCaps" Name="PART_TextDisplayName" ClipToBounds="False"
                                                                   FontSize="{DynamicResource FontSizeLargest}" Margin="0,0,10,0"
                                                                   FontWeight="Bold"
                                                                   TextWrapping="Wrap" VerticalAlignment="Center" Foreground="White" Width="Auto" />
                                                    </Border>
                                                </DockPanel>

                                                

                                                <ContentControl x:Name="NewsViewer_PlayersInGameViewerControl"
                                                            Visibility="{PluginSettings Plugin=NewsViewer, Path=PlayersCountAvailable, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                            Margin="0,0,10,0"
                                                            DockPanel.Dock="Left" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                    <ContentControl.Resources>
                                                        <Style TargetType="Button" BasedOn="{StaticResource PlayersCountButtonStyle}" />
                                                    </ContentControl.Resources>
                                                </ContentControl>

                                                <ContentControl x:Name="GogSecondClassWatcher_SecondClassWatcherControl"
                                                            VerticalAlignment="Center"
                                                            Visibility="{PluginSettings Plugin=GogSecondClassWatcher, Path=IsControlVisible, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                            Margin="0,0,10,0"/>
                                            </WrapPanel>

                                            <DockPanel Grid.Row="1" Name="PART_ElemDevelopers"
                                                       VerticalAlignment="Top" Margin="0,15">
                                                <Border Style="{DynamicResource DetailsPanelRoundedBorder}" Margin="0">
                                                    <Border.Resources>
                                                        <Style TargetType="Button" BasedOn="{StaticResource PropertyItemButton}" />
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Margin" Value="0,0,20,0" />
                                                        </Style>
                                                    </Border.Resources>
                                                    <Border.Background>
                                                        <SolidColorBrush Color="{DynamicResource ControlsBackgroundColor}" Opacity="0.75"/>
                                                    </Border.Background>
                                                    <WrapPanel Margin="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                        <Label Margin="0,4,10,0" FontWeight="Medium" Content="{DynamicResource LOCDeveloperLabel}" Foreground="White" VerticalAlignment="Top"/>
                                                        <ItemsControl VerticalAlignment="Center" Name="PART_ItemsDevelopers" AlternationCount="{Binding RelativeSource={RelativeSource Self}, Path=Items.Count}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock x:Name="commaTextBlock" Text=", "/>
                                                                        <TextBlock Text="{Binding }"/>
                                                                    </StackPanel>
                                                                    <DataTemplate.Triggers>
                                                                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                                                            <Setter Property="Visibility" TargetName="commaTextBlock" Value="Collapsed" />
                                                                        </Trigger>
                                                                    </DataTemplate.Triggers>
                                                                </DataTemplate>

                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </WrapPanel>
                                                </Border>
                                            </DockPanel>

                                            <DockPanel Grid.Row="2" Name="PART_ElemPublishers"
                                                       VerticalAlignment="Top">
                                                <Border Style="{DynamicResource DetailsPanelRoundedBorder}" Margin="0">
                                                    <Border.Resources>
                                                        <Style TargetType="Button" BasedOn="{StaticResource PropertyItemButton}" />
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Margin" Value="0,15,20,0" />
                                                        </Style>
                                                    </Border.Resources>
                                                    <Border.Background>
                                                        <SolidColorBrush Color="{DynamicResource ControlsBackgroundColor}" Opacity="0.75"/>
                                                    </Border.Background>
                                                    <WrapPanel Margin="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <Label Margin="0,4,10,0"
                                                   FontWeight="Medium"
                                                   Content="{DynamicResource LOCPublisherLabel}"
                                                   Foreground="White" VerticalAlignment="Top"/>
                                                        <ItemsControl VerticalAlignment="Center" Name="PART_ItemsPublishers">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                                    </WrapPanel>
                                                </Border>
                                            </DockPanel>
                                            <!-- DockPanel Grid.Row="3" Name="PART_ElemPlatform"
                                                       VerticalAlignment="Top" Margin="0,15,0,0">
                                                <Label Margin="0,4,10,0"
                                                   FontWeight="Medium"
                                                   Content="{DynamicResource LOCGamePlatformTitle}"
                                                   Foreground="White" VerticalAlignment="Top"/>
                                                <ItemsControl VerticalAlignment="Center" Name="PART_ItemsPlatforms">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </ -->

                                            <DockPanel x:Name="ScoresPanel" Grid.Row="3" VerticalAlignment="Stretch" Margin="0,15,0,0">
                                                <Border Padding="20,14,20,0" Height="Auto" Margin="0">
                                                    <Border.Style>
                                                        <Style BasedOn="{StaticResource DetailsPanelRoundedBorder}" TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemCommunityScore, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemCriticScore, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemUserScore, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.Resources>
                                                        <Style TargetType="Button" BasedOn="{StaticResource PropertyItemButton}" />
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Margin" Value="0,0,20,14" />
                                                        </Style>
                                                    </Border.Resources>
                                                    <Border.Background>
                                                        <SolidColorBrush Color="{DynamicResource ControlsBackgroundColor}" Opacity="0.75"/>
                                                    </Border.Background>
                                                    <WrapPanel Margin="0" HorizontalAlignment="Left" VerticalAlignment="Center">

                                                        <StackPanel x:Name="PART_ElemCommunityScore" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCCommunityScore}" />
                                                            <TextBlock x:Name="PART_TextCommunityScore" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" Margin="0,5,0,0" />
                                                            <!-- StackPanel Orientation="Horizontal"
                                                                Visibility="{Binding Visibility, ElementName=PART_TextCommunityScore}">
                                                                <ContentControl Content="{Binding Game.CommunityScore}"
                                                                        Padding="6,0,0,4"
                                                                        HorizontalAlignment="Left"
                                                                        VerticalAlignment="Center"
                                                                        Background="Transparent"
                                                                        Foreground="{Binding Foreground, ElementName=PART_TextCommunityScore}"
                                                                        Height="{Binding ActualHeight, ElementName=PART_TextCommunityScore}"
                                                                        ContentTemplate="{StaticResource FiveStarScoreTemplate}"/>
                                                            </ -->
                                                        </StackPanel>

                                                        <StackPanel x:Name="PART_ElemCriticScore" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCCriticScore}" />
                                                            <TextBlock x:Name="PART_TextCriticScore" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" Margin="0,5,0,0" />
                                                            <!-- StackPanel Orientation="Horizontal"
                                                                Visibility="{Binding Visibility, ElementName=PART_TextCriticScore}">
                                                                <ContentControl Content="{Binding Game.CriticScore}"
                                                                        Padding="6,0,0,4"
                                                                        HorizontalAlignment="Left"
                                                                        VerticalAlignment="Center"
                                                                        Background="Transparent"
                                                                        Foreground="{Binding Foreground, ElementName=PART_TextCriticScore}"
                                                                        Height="{Binding ActualHeight, ElementName=PART_TextCriticScore}"
                                                                        ContentTemplate="{StaticResource FiveStarScoreTemplate}"/>
                                                            </ -->
                                                        </StackPanel>

                                                        <StackPanel x:Name="PART_ElemUserScore" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCUserScore}" />
                                                            <TextBlock x:Name="PART_TextUserScore" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" Margin="0,5,0,0" />
                                                            <!-- StackPanel Orientation="Horizontal"
                                                                            Visibility="{Binding Visibility, ElementName=PART_TextUserScore}">                                                                
                                                                <ContentControl Content="{Binding Game.UserScore}"
                                                                        Padding="6,0,0,4"
                                                                        HorizontalAlignment="Left"
                                                                        VerticalAlignment="Center"
                                                                        Background="Transparent"
                                                                        Foreground="{Binding Foreground, ElementName=PART_TextUserScore}"
                                                                        Height="{Binding ActualHeight, ElementName=PART_TextUserScore}"
                                                                        ContentTemplate="{StaticResource FiveStarScoreTemplate}"/>
                                                            </ -->
                                                        </StackPanel>
                                                    </WrapPanel>
                                                </Border>
                                            </DockPanel>

                                            <DockPanel x:Name="ReleaseInfoPanel" Grid.Row="4" VerticalAlignment="Stretch" Margin="0,15,0,0">
                                                <Border Padding="20,14,20,0" Height="Auto" Margin="0">
                                                    <Border.Style>
                                                        <Style BasedOn="{StaticResource DetailsPanelRoundedBorder}" TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemSource, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemReleaseDate, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemSeries, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemVersion, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemAgeRating, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemRegion, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.Resources>
                                                        <Style TargetType="Button" BasedOn="{StaticResource PropertyItemButton}" />
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Margin" Value="0,0,20,14" />
                                                        </Style>
                                                    </Border.Resources>
                                                    <Border.Background>
                                                        <SolidColorBrush Color="{DynamicResource ControlsBackgroundColor}" Opacity="0.75"/>
                                                    </Border.Background>
                                                    <WrapPanel Margin="0" HorizontalAlignment="Left" VerticalAlignment="Center">

                                                        <StackPanel x:Name="PART_ElemReleaseDate" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCGameReleaseDateTitle}" />
                                                            <Button x:Name="PART_ButtonReleaseDate" />
                                                        </StackPanel>

                                                        <StackPanel x:Name="PART_ElemSource" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCSourceLabel}" />
                                                            <Button x:Name="PART_ButtonSource" />
                                                        </StackPanel>

                                                        <!-- StackPanel x:Name="PART_ElemSeries" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCSeriesLabel}" />
                                                            <ItemsControl x:Name="PART_ItemsSeries" />
                                                        </ -->
                                                        <StackPanel x:Name="PART_ElemSeries" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCSeriesLabel}" VerticalAlignment="Top"/>
                                                            <WrapPanel Margin="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                                <ItemsControl VerticalAlignment="Center" Name="PART_ItemsSeries">
                                                                    <ItemsControl.ItemsPanel>
                                                                        <ItemsPanelTemplate>
                                                                            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                                        </ItemsPanelTemplate>
                                                                    </ItemsControl.ItemsPanel>
                                                                </ItemsControl>
                                                            </WrapPanel>
                                                        </StackPanel>

                                                        <StackPanel x:Name="PART_ElemVersion" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCVersionLabel}" />
                                                            <Button x:Name="PART_ButtonVersion" VerticalAlignment="Center" />
                                                        </StackPanel>

                                                        <!-- StackPanel x:Name="PART_ElemAgeRating" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCAgeRatingLabel}" />
                                                            <ItemsControl x:Name="PART_ItemsAgeRatings" />
                                                        </ -->

                                                        <StackPanel x:Name="PART_ElemRegion" Margin="0,0,20,14">
                                                            <Label Content="{DynamicResource LOCRegionLabel}" />
                                                            <ItemsControl x:Name="PART_ItemsRegions" Margin="0,5,0,0" />
                                                        </StackPanel>
                                                    </WrapPanel>
                                                </Border>
                                            </DockPanel>
                                            <DockPanel x:Name="InstallInfoPanel" Grid.Row="5" VerticalAlignment="Stretch" Margin="0,15,0,0">
                                                <Border Padding="20,14,20,0" Margin="0">
                                                    <Border.Style>
                                                        <Style BasedOn="{StaticResource DetailsPanelRoundedBorder}" TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemRecentActivity, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemRecentActivity, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemAdded, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemInstallSize, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ElemInstallDirectory, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.Resources>
                                                        <Style TargetType="Button" BasedOn="{StaticResource PropertyItemButton}" />
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Margin" Value="0,0,20,14" />
                                                        </Style>
                                                    </Border.Resources>
                                                    <Border.Background>
                                                        <SolidColorBrush Color="{DynamicResource ControlsBackgroundColor}" Opacity="0.75"/>
                                                    </Border.Background>
                                                    <WrapPanel>
                                                        <StackPanel Name="PART_ElemRecentActivity">
                                                            <Label Content="{DynamicResource LOCRecentActivityLabel}" />
                                                            <TextBlock Name="PART_TextRecentActivity" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" Margin="0,5,0,0" />
                                                        </StackPanel>

                                                        <StackPanel Name="PART_ElemAdded">
                                                            <Label Content="{DynamicResource LOCDateAddedLabel}" />
                                                            <TextBlock Name="PART_TextAdded" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" Margin="0,5,0,0" />
                                                        </StackPanel>

                                                        <StackPanel Name="PART_ElemInstallSize">
                                                            <Label Content="{DynamicResource LOCInstallSizeLabel}" />
                                                            <TextBlock Name="PART_TextInstallSize" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" Margin="0,5,0,0" />
                                                        </StackPanel>

                                                        <StackPanel Name="PART_ElemInstallDirectory">
                                                            <Label Content="{DynamicResource LOCGameInstallDirTitle}" />
                                                            <Button Name="PART_ButtonInstallDirectory" VerticalAlignment="Center" />
                                                        </StackPanel>

                                                    </WrapPanel>
                                                </Border>
                                            </DockPanel>
                                            <ContentControl Grid.Row="6" x:Name="CheckLocalizations_PluginFlags" Height="30" Margin="0,10,0,0"
                                                            Visibility="{PluginSettings Plugin=CheckLocalizations, Path=EnableIntegrationFlags, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                            HorizontalAlignment="Left" VerticalAlignment="Center"
                                                            Opacity="0.60" />

                                            <ContentControl Grid.Row="7" VerticalAlignment="Bottom"
                                                        x:Name="LibraryManagement_PluginFeaturesIconList" Height="35" Margin="0,10,0,0"
                                                        HorizontalAlignment="Left"
                                                        Opacity="0.75"
                                                        Visibility="{PluginSettings Plugin=LibraryManagement, Path=EnableIntegrationFeatures, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <WrapPanel Margin="0,0,0,0" Grid.Row="8" VerticalAlignment="Bottom">
                                                <Button Margin="0,15,20,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="PART_ButtonPlayAction" Style="{DynamicResource PlayButtonViews}" Background="#FFB07000" />
                                                <Button Margin="0,15,10,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="PART_ButtonContextAction" Style="{DynamicResource ContextButtonViews}" />
                                                <Button Name="PART_ButtonMoreActions" Content="&#xE712;" FontFamily="Segoe MDL2 Assets" Margin="0,15,10,0" Style="{DynamicResource MoreActionButtonViews}" />
                                                <Button x:Name="PART_ButtonEditGame" Content="&#xE70F;" FontFamily="Segoe MDL2 Assets" Margin="0,15,10,0" Focusable="False" Style="{DynamicResource MoreActionButtonViews}" />
                                                <ToggleButton Name="GameFavoriteToggleButton" Margin="0,15,10,0" Focusable="False"
                                                              Style="{DynamicResource PanelFavoriteToggleButton}"
                                                              Height="54"
                                                              Visibility="{PluginStatus Plugin=felixkmh_Extras_Plugin}"
                                                              IsChecked="{PluginSettings Plugin=ThemeExtras, Path=Game.Favorite, Mode=TwoWay, FallbackValue={StaticResource False}}" Width="54"/>

                                                <ContentControl x:Name="PlayState_GameStateSwitchControl" Margin="0,15,10,0"
                                                                Height="54"
                                                                Visibility="{PluginSettings Plugin=PlayState, Path=IsControlVisible, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                                <ContentControl x:Name="ScreenshotsVisualizer_PluginButton" Margin="0,15,10,0"
                                                                Height="54" MaxWidth="150" DockPanel.Dock="Left"
                                                                Visibility="{PluginSettings Plugin=ScreenshotsVisualizer, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                                <ContentControl x:Name="HowLongToBeat_PluginButton" Height="54" VerticalAlignment="Center" Margin="0,15,10,0"
                                                                Visibility="{PluginSettings Plugin=HowLongToBeat, Path=EnableIntegrationButton, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                                <ContentControl x:Name="CheckDlc_PluginButton" Height="54" VerticalAlignment="Center" Margin="0,15,10,0"
                                                                Visibility="{PluginSettings Plugin=CheckDlc, Path=EnableIntegrationButton, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                                <ContentControl x:Name="SuccessStory_PluginButton" Height="54" VerticalAlignment="Center" Margin="0,15,10,0"
                                                                Visibility="{PluginSettings Plugin=SuccessStory, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                                <ContentControl x:Name="GameActivity_PluginButton" Height="54" VerticalAlignment="Center" Margin="0,15,10,0"
                                                                Visibility="{PluginSettings Plugin=GameActivity, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                                <ContentControl x:Name="SystemChecker_PluginButton" Height="54" VerticalAlignment="Center" Margin="0,15,10,0"
                                                                Visibility="{PluginSettings Plugin=SystemChecker, Path=EnableIntegrationButton, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                                <ContentControl x:Name="CheckLocalizations_PluginButton" Height="54" VerticalAlignment="Center" Margin="0,15,10,0"
                                                                Visibility="{PluginSettings Plugin=CheckLocalizations, Path=EnableIntegrationButton, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Width="54" />
                                            </WrapPanel>
                                        </Grid>
                                        <Grid Grid.Column="2">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <Grid Grid.Row="0" Tag="{DynamicResource DetailsViewAllowUseOfLogos}"
                                                        Visibility="{Binding RelativeSource={RelativeSource Self}, Path=Tag, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                        Margin="15">
                                                    <Border MinHeight="{PluginSettings Plugin=ExtraMetadataLoader, Path=LogoMaxHeight, FallbackValue=0}"
                                                            Visibility="{PluginSettings Plugin=ExtraMetadataLoader, Path=IsLogoAvailable, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <ContentControl x:Name="ExtraMetadataLoader_LogoLoaderControl" />
                                                </Grid>

                                                <Border Grid.Row="1" Background="Transparent"
                                                        VerticalAlignment="Bottom"
                                                        Visibility="{Binding ElementName=CoverGridContainer, Path=Visibility}"
                                                        HorizontalAlignment="Center"
                                                        Width="{Binding ElementName=CoverGridContainer, Path=ActualWidth}"
                                                        Height="{Binding ElementName=CoverGridContainer, Path=ActualHeight}"
                                                        Padding="0.5">
                                                    <Border Background="Transparent"
                                                        CornerRadius="{DynamicResource DetailsViewCoverCornerRadius}"
                                                        BorderThickness="8" BorderBrush="#0d0d0d">
                                                        <Border.Effect>
                                                            <DropShadowEffect BlurRadius="15" RenderingBias="Performance"
                                                                          ShadowDepth="0" Color="#0d0d0d" Opacity="0.3" />
                                                        </Border.Effect>
                                                    </Border>
                                                </Border>

                                                <Grid Grid.Row="1" Name="CoverGridContainer"
                                                      VerticalAlignment="Bottom"
                                                      Visibility="{Settings DetailsVisibility.CoverImage, FallbackValue=Visible, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                      HorizontalAlignment="Center">
                                                    <Grid.Style>
                                                        <Style TargetType="Grid">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ImageCover, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=BackgroundChanger_PluginCoverImage, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Grid.Style>
                                                    <Border Name="Mask"
                                                            Background="White"
                                                            CornerRadius="{DynamicResource DetailsViewCoverCornerRadius}" />
                                                    <Border>
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ElementName=BackgroundChanger_PluginCoverImage, Path=Visibility, TargetNullValue=Collapsed}" Value="Visible">
                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <Image Name="PART_ImageCover" Height="{Settings GameDetailsCoverHeight}" 
                                                               StretchDirection="Both" Stretch="Uniform" 
                                                               RenderOptions.BitmapScalingMode="Fant" />
                                                    </Border>

                                                    <ContentControl x:Name="BackgroundChanger_PluginCoverImage"
                                                        Height="{Settings GameDetailsCoverHeight}">
                                                        <ContentControl.Style>
                                                            <Style TargetType="ContentControl">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=HasDataCover, FallbackValue=False}" Value="True" />
                                                                            <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=EnableCoverImage, FallbackValue=False}" Value="True" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </ContentControl.Style>
                                                    </ContentControl>
                                                    <Grid.OpacityMask>
                                                        <VisualBrush Visual="{Binding ElementName=Mask}" />
                                                    </Grid.OpacityMask>
                                                </Grid>
                                            </Grid>
                                        </Grid>

                                        <!--GridConverterGridPanelCloseButton-->
                                    </Grid>
                                    <Border Height="40" Visibility="Hidden" />
                                </StackPanel>

                                <Border Grid.Row="1" Margin="0,0,0,0"
                                        Padding="40,12,40,12"
                                        MinHeight="84"
                                        Background="{DynamicResource DetailsViewPanelBackgroundBrush}">
                                    <Grid x:Name="GridFooter" MaxWidth="{DynamicResource DetailsViewDetailsMaxWidth}">
                                        <StackPanel Orientation="Horizontal">

                                            <WrapPanel Width="{Binding ElementName=GridFooter, Path=ActualWidth}" MaxWidth="{DynamicResource DetailsViewDetailsMaxWidth}" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                <WrapPanel.Resources>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                                                        <Setter Property="Foreground" Value="{DynamicResource DetailsViewPanelTextForegroundBrush}" />
                                                    </Style>
                                                    <Style TargetType="TextBlock" x:Key="Icon">
                                                        <Setter Property="Foreground" Value="{DynamicResource DetailsViewPanelIconForegroundBrush}" />
                                                        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
                                                    </Style>
                                                    <Style TargetType="Label" x:Key="LabelHeaderStyle">
                                                        <Setter Property="Foreground" Value="{DynamicResource DetailsViewDetailsLabelForegroundBrush}" />
                                                        <Setter Property="FontWeight" Value="Medium" />
                                                        <Setter Property="Margin" Value="-5,0,0,0" />
                                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                                    </Style>
                                                </WrapPanel.Resources>
                                                <DockPanel Visibility="{Binding Visibility, ElementName=PART_ElemLibrary, Mode=OneWay}" Margin="0,0,15,0">
                                                    <Viewbox Stretch="Uniform" Height="40">
                                                        <TextBlock Text="&#xE1D3;" Style="{DynamicResource Icon}" />
                                                    </Viewbox>
                                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <Label Name="PART_ElemLibrary" Content="{DynamicResource LOCGameProviderTitle}" Style="{DynamicResource LabelHeaderStyle}" />
                                                        <Button Name="PART_ButtonLibrary" Style="{StaticResource PropertyItemButton}" Margin="0" />
                                                    </StackPanel>
                                                </DockPanel>

                                                <DockPanel Margin="0,0,15,0">
                                                    <DockPanel.Style>
                                                        <Style TargetType="DockPanel">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ElementName=PART_ButtonCompletionStatus, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ElementName=ThemeExtras_SettableCompletionStatus, Path=Visibility}" Value="Visible">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Settings DetailsVisibility.CompletionStatus}" Value="False">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </DockPanel.Style>
                                                    <Viewbox Stretch="Uniform" Height="40">
                                                        <TextBlock Text="&#xF16A;" Style="{DynamicResource Icon}" />
                                                    </Viewbox>

                                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <Label Content="{DynamicResource LOCCompletionStatus}" Style="{DynamicResource LabelHeaderStyle}" />

                                                        <WrapPanel>
                                                            <Border Tag="{PluginStatus Plugin=felixkmh_Extras_Plugin, Status=Installed}"
                                                                    Visibility="{Binding RelativeSource={RelativeSource Self}, Path=Tag, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                <Button Name="PART_ButtonCompletionStatus" Style="{StaticResource PropertyItemButton}" Margin="0" />
                                                            </Border>

                                                            <ContentControl Visibility="{PluginStatus Plugin=felixkmh_Extras_Plugin, Status=Installed}" Margin="0,-4,10,0"
                                                                            x:Name="ThemeExtras_SettableCompletionStatus">
                                                                <ContentControl.Resources>
                                                                    <Style TargetType="UserControl" BasedOn="{StaticResource ThemeExtrasUserControlCompStatusStyle}" />
                                                                </ContentControl.Resources>
                                                            </ContentControl>
                                                        </WrapPanel>
                                                    </StackPanel>
                                                </DockPanel>

                                                <DockPanel Visibility="{Binding Visibility, ElementName=PART_ElemLastPlayed, Mode=OneWay}" Margin="0,0,15,0">
                                                    <Viewbox Stretch="Uniform" Height="35">
                                                        <TextBlock Text="&#xE163;" Style="{DynamicResource Icon}" />
                                                    </Viewbox>
                                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <Label Name="PART_ElemLastPlayed" Content="{DynamicResource LOCLastPlayed}" Style="{DynamicResource LabelHeaderStyle}" />
                                                        <TextBlock Name="PART_TextLastActivity" MinWidth="85"/>
                                                    </StackPanel>
                                                </DockPanel>
                                                <DockPanel Visibility="{Binding Visibility, ElementName=PART_ElemPlayTime, Mode=OneWay}" Margin="0,0,15,0">
                                                    <Viewbox Stretch="Uniform" Height="35" Margin="0">
                                                        <TextBlock Text="&#xE121;" Style="{DynamicResource Icon}" Margin="0,-1,0,0" />
                                                    </Viewbox>
                                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <Label Name="PART_ElemPlayTime" Content="{DynamicResource LOCTimePlayed}" Style="{DynamicResource LabelHeaderStyle}" />
                                                        <TextBlock Name="PART_TextPlayTime"/>
                                                    </StackPanel>
                                                </DockPanel>

                                                <DockPanel Margin="0,0,15,0">
                                                    <DockPanel.Style>
                                                        <Style TargetType="{x:Type DockPanel}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{PluginSettings Plugin=SuccessStory, Path=HasData, FallbackValue=False}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </DockPanel.Style>
                                                    <Viewbox Stretch="Uniform" Height="30">
                                                        <TextBlock Text="&#xE728;" Style="{DynamicResource Icon}" />
                                                    </Viewbox>

                                                    <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                                                        <Label Content="{DynamicResource LOCSuccessStoryAchievements}" Style="{DynamicResource LabelHeaderStyle}" />
                                                        <DockPanel>
                                                            <DockPanel MinWidth="40">
                                                                <TextBlock Text="{PluginSettings Plugin=SuccessStory, Path=Unlocked, FallbackValue='-'}"/>
                                                                <TextBlock Text="/"/>
                                                                <TextBlock Text="{PluginSettings Plugin=SuccessStory, Path=Total, FallbackValue='-'}"/>
                                                            </DockPanel>
                                                            <ProgressBar x:Name="progressBar" FontSize="10" Margin="5,0,0,0"
                                                            Maximum="{PluginSettings Plugin=SuccessStory, Path=Total, FallbackValue=0}" Value="{PluginSettings Plugin=SuccessStory, Path=Unlocked, FallbackValue=0}" HorizontalAlignment="Left"
                                                            Background="#37393e" Foreground="{DynamicResource DetailsViewPanelIconForegroundBrush}" Width="60" Height="10" Style="{DynamicResource RoundedProgressBar}">
                                                                <ProgressBar.Resources>
                                                                    <Style x:Key="RoundedProgressBar" TargetType="ProgressBar">
                                                                        <Setter Property="Background" Value="{DynamicResource NormalBrushDark}"/>
                                                                        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}"/>
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="{x:Type ProgressBar}">
                                                                                    <Border BorderThickness="{TemplateBinding BorderThickness}"
								                                                CornerRadius="{DynamicResource ControlCornerRadius}">
                                                                                        <Grid SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                                                                            <Rectangle Fill="{TemplateBinding Background}" RadiusY="4" RadiusX="4"/>
                                                                                            <Rectangle x:Name="PART_Track" Margin="0" RadiusY="4" RadiusX="4"/>
                                                                                            <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left" VerticalAlignment="Stretch">
                                                                                                <Grid>
                                                                                                    <Rectangle Fill="{TemplateBinding Foreground}" RadiusY="4" RadiusX="4"/>
                                                                                                </Grid>
                                                                                            </Decorator>
                                                                                        </Grid>
                                                                                    </Border>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                    </Style>
                                                                </ProgressBar.Resources>
                                                            </ProgressBar>
                                                        </DockPanel>
                                                    </StackPanel>
                                                </DockPanel>

                                                <DockPanel Margin="0,0,0,0">
                                                    <DockPanel.Style>
                                                        <Style TargetType="{x:Type DockPanel}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{PluginSettings Plugin=HowLongToBeat, Path=HasData, FallbackValue=False}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </DockPanel.Style>
                                                    <Viewbox Stretch="Uniform" Height="30">
                                                        <Path Stretch="Fill" Fill="{DynamicResource DetailsViewPanelIconForegroundBrush}" Data="m 157.80154 541.46802 v -400 h 97.14286 97.14286 
                                                    v 238.57143 238.57143 h 40 40 V 380.03945 141.46802 h 97.14285 97.14286 v 400 400 
                                                    h -97.14286 -97.14285 v -81.42857 -81.42857 h -40 -40 v 81.42857 81.42857 H 254.9444 157.80154 Z"/>
                                                    </Viewbox>
                                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <Label Content="HowLongToBeat" Style="{DynamicResource LabelHeaderStyle}" />
                                                        <ContentControl x:Name="HowLongToBeat_PluginProgressBar" Margin="0,0,0,0" VerticalAlignment="Center" Width="110" HorizontalAlignment="Left"/>
                                                    </StackPanel>
                                                </DockPanel>
                                            </WrapPanel>
                                        </StackPanel>
                                    </Grid>

                                </Border>
                                <Border Grid.Row="2" Margin="0,0,0,0"
                                        Padding="40,12,40,12"
                                        MinHeight="84"
                                        Background="{DynamicResource PopupBackgroundBrush}">
                                    <Grid x:Name="GridLinks" MaxWidth="{DynamicResource DetailsViewDetailsMaxWidth}">
                                        <StackPanel Orientation="Horizontal">

                                            <StackPanel x:Name="PART_ElemLinks" Margin="0,0,0,20">
                                                <Grid MinHeight="45">
                                                    <ItemsControl ItemsSource="{Binding Game.Links}" Margin="5,0,-5,0">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate DataType="{x:Type Link}">
                                                                <Button Content="{Binding Name}"
                                                                                            Style="{StaticResource PropertyItemLinkRoundedPanelButton}"
                                                                                            ToolTip="{Binding Url}"
                                                                                            Command="{Binding DataContext.OpenLinkCommand, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}"
                                                                                            CommandParameter="{Binding}"/>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Grid>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Grid Grid.Row="3" Background="{DynamicResource WindowBackgourndBrush}">
                                    <Grid.Resources>
                                        <Style TargetType="Border" x:Key="DetailsPanelRoundedBorder">
                                            <Setter Property="CornerRadius" Value="3" />
                                            <Setter Property="Background" Value="{DynamicResource DetailsViewPanelBackgroundBrush}" />
                                            <Setter Property="Padding" Value="20,14,20,14" />
                                            <Setter Property="Margin" Value="0,0,0,32" />
                                        </Style>
                                    </Grid.Resources>

                                    <Border Background="{DynamicResource NoiseBrush}"
                                            Opacity="{DynamicResource PanelsNoiseBackgroundOpacity}"/>

                                    <TabControl Style="{DynamicResource TabControlDetailsView}" Margin="40,0,40,0"
                                                Focusable="False">
                                        <TabControl.Resources>
                                            <!-- Style x:Key="PanelLabel" TargetType="Label">
                                                <Setter Property="FontSize" Value="{DynamicResource FontSizeLarger}" />
                                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                                                <Setter Property="FontWeight" Value="Medium" />
                                                <Setter Property="FontSize" Value="{DynamicResource FontSizeLarger}" />
                                            </ -->
                                        </TabControl.Resources>
                                        <TabItem Header="{DynamicResource LOCSettingsGameDetailsLabel}" Width="0" Height="0">
                                            <Grid Margin="0,32,0,0" MaxWidth="{DynamicResource DetailsViewDetailsMaxWidth}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="52" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <StackPanel.Resources>
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Margin" Value="0,0,0,0" />
                                                        </Style>
                                                        <Style TargetType="Label">
                                                            <Setter Property="Foreground" Value="{DynamicResource DetailsViewPanelTextForegroundBrush}" />
                                                            <Setter Property="FontWeight" Value="Normal" />
                                                            <Setter Property="HorizontalAlignment" Value="Left" />
                                                            <Setter Property="Padding" Value="0" />
                                                            <Setter Property="Margin" Value="0,3,0,0" />
                                                        </Style>
                                                    </StackPanel.Resources>
                                                    <Border>
                                                        <Border.Style>
                                                            <Style TargetType="{x:Type Border}">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Visibility, ElementName=PART_TextNotes}" Value="Visible">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{PluginSettings FallbackValue=False, Plugin=PlayNotes, Path=IsControlVisible}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <Border Style="{DynamicResource DetailsPanelRoundedBorder}">
                                                            <StackPanel>
                                                                <Label Style="{DynamicResource PanelLabel}"
                                                                       Content="{DynamicResource LOCNotesLabel}" />
                                                                <TextBox x:Name="PART_TextNotes" IsReadOnly="True"
                                                                     MaxHeight="250"
                                                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                                                     BorderThickness="0"
                                                                     AcceptsReturn="True" TextWrapping="Wrap"
                                                                     Foreground="{DynamicResource DetailsViewPanelTextForegroundBrush}"
                                                                     Margin="-1,5,-1,5" Padding="0" Background="Transparent" FontWeight="Normal"/>

                                                                <ContentControl x:Name="PlayNotes_NotesViewerControl"
                                                                                Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=PlayNotes, Path=IsControlVisible}"
                                                                                Margin="0,10,0,0" />
                                                            </StackPanel>
                                                        </Border>
                                                    </Border>

                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=NewsViewer, Path=ReviewsAvailable}">
                                                        <StackPanel>
                                                            <Label Style="{DynamicResource PanelLabel}"
                                                                   Content="{DynamicResource LOC_NewsViewer_NewsViewerControl_NewsLabel}" />
                                                            <ContentControl x:Name="NewsViewer_NewsViewerControl" Margin="0,5,0,5" FontWeight="Normal"/>
                                                        </StackPanel>
                                                    </Border>
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition MaxHeight="700" />
                                                        </Grid.RowDefinitions>
                                                        <Border Style="{DynamicResource DetailsPanelRoundedBorder}" Grid.Row="0">
                                                            <StackPanel>
                                                                <ScrollViewer MinHeight="0"
                                                          MaxHeight="{DynamicResource DetailsViewDescriptionMaxHeight}"
                                                          VerticalScrollBarVisibility="Auto" Height="Auto" Margin="0">
                                                                    <HtmlTextView
                                                    x:Name="PART_HtmlDescription"
                                                    HtmlFontSize="{DynamicResource FontSize}"
                                                    HtmlFontFamily="{DynamicResource FontFamily}"
                                                    HtmlForeground="#afafaf"
                                                    LinkForeground="{DynamicResource PropertyItemButtonTextColor}"                                      
                                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                    ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                                                                </ScrollViewer>
                                                            </StackPanel>
                                                        </Border>
                                                    </Grid>
                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=ReviewViewer, Path=IsControlVisible}">
                                                        <StackPanel>
                                                            <Label Style="{DynamicResource PanelLabel}"
                                                                   Content="{DynamicResource LOCReview_Viewer_ReviewsLabel}" />
                                                            <ContentControl x:Name="ReviewViewer_ReviewsControl" Margin="0,5,0,5" FontWeight="Normal"/>
                                                        </StackPanel>
                                                    </Border>


                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{Binding Tag, ElementName=GameRelationsVisibilityProxy}">
                                                        <StackPanel>

                                                            <TextBlock x:Name="GameRelationsVisibilityProxy" Visibility="Collapsed">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="{x:Type TextBlock}">
                                                                        <Setter Property="Tag" Value="Collapsed" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Visibility, ElementName=Panel_GameRelations_SimilarGamesControl}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Visibility, ElementName=Panel_GameRelations_SameSeriesControl}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Visibility, ElementName=Panel_GameRelations_SameDeveloperControl}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Visibility, ElementName=Panel_GameRelations_SamePublisherControl}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>

                                                            <Label Style="{DynamicResource PanelLabel}"
                                                                   Content="{DynamicResource LOCGameRelations_RelatedGamesLabel}" />
                                                            <Expander x:Name="Panel_GameRelations_SimilarGamesControl"
                                                                      Header="{DynamicResource LOCGameRelations_SimilarGamesLabel}"
                                                                      IsExpanded="True"
                                                                      Margin="0,10,0,0"
                                                                      Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=GameRelations, Path=SimilarGamesControlSettings.IsVisible}">
                                                                <ContentControl x:Name="GameRelations_SimilarGamesControl" Margin="0,10,0,0" />
                                                            </Expander>

                                                            <Expander x:Name="Panel_GameRelations_SameSeriesControl"
                                                                      Header="{DynamicResource LOCGameRelations_SameSeriesLabel}"
                                                                      IsExpanded="True"
                                                                      Margin="0,10,0,0"
                                                                      Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=GameRelations, Path=SameSeriesControlSettings.IsVisible}">
                                                                <ContentControl x:Name="GameRelations_SameSeriesControl" Margin="0,10,0,0" />
                                                            </Expander>

                                                            <Expander x:Name="Panel_GameRelations_SameDeveloperControl"
                                                                      Header="{DynamicResource LOCGameRelations_SameDeveloperLabel}"
                                                                      IsExpanded="True"
                                                                      Margin="0,10,0,0"
                                                                      Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=GameRelations, Path=SameDeveloperControlSettings.IsVisible}">
                                                                <ContentControl x:Name="GameRelations_SameDeveloperControl" Margin="0,10,0,0" />
                                                            </Expander>

                                                            <Expander x:Name="Panel_GameRelations_SamePublisherControl"
                                                                      Header="{DynamicResource LOCGameRelations_SamePublisherLabel}"
                                                                      IsExpanded="True"
                                                                      Margin="0,10,0,0"
                                                                      Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=GameRelations, Path=SamePublisherControlSettings.IsVisible}">
                                                                <ContentControl x:Name="GameRelations_SamePublisherControl" Margin="0,10,0,0" />
                                                            </Expander>
                                                        </StackPanel>
                                                    </Border>

                                                </StackPanel>

                                                <StackPanel Grid.Column="2">

                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{Binding ElementName=VideoScreenshotsTabControlVisibilityProxy, Path=Tag}">
                                                        <TabControl>
                                                            <TabItem x:Name="SteamScreenshotsTabItem"
                                                                     Header="{DynamicResource LOC_SteamScreenshots_SteamScreenshotsLabel}"
                                                                     Visibility="{PluginSettings Plugin=SteamScreenshots, Path=IsControlVisible, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Height="0" Width="0">
                                                                
                                                                <Grid VerticalAlignment="Center" Margin="0,15,0,0">
                                                                    
                                                                    <Viewbox Stretch="Uniform" Visibility="{Binding ElementName=SteamScreenshotsTabItem, Path=Visibility}">
                                                                        <Rectangle Width="16" Height="9" Fill="Transparent" IsHitTestVisible="False" />
                                                                    </Viewbox>
                                                                    <ContentControl x:Name="SteamScreenshots_SteamScreenshotsViewControl" />

                                                                    <TextBlock x:Name="VideoScreenshotsTabControlVisibilityProxy"
                                                                               Visibility="Collapsed">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Tag" Value="Collapsed" />
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding ElementName=SteamScreenshotsTabItem, Path=Visibility}" Value="Visible">
                                                                                        <Setter Property="Tag" Value="Visible" />
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Grid>
                                                            </TabItem>
                                                        </TabControl>
                                                    </Border>

                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{PluginSettings Plugin=HowLongToBeat, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <StackPanel>
                                                            <Label Style="{DynamicResource PanelLabel}" FontSize="{DynamicResource FontSizeLarge}" Margin="0,0,0,10"
                                                                   Content="HowLongToBeat" />
                                                            <ContentControl ContentTemplate="{DynamicResource HltbDataGridTemplate}" />
                                                        </StackPanel>
                                                    </Border>

                                                    <Border>
                                                        <Border.Style>
                                                            <Style BasedOn="{StaticResource DetailsPanelRoundedBorder}" TargetType="Border">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ElementName=PART_ElemCategories, Path=Visibility}" Value="Visible">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ElementName=PART_ElemFeatures, Path=Visibility}" Value="Visible">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ElementName=PART_ElemGenres, Path=Visibility}" Value="Visible">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ElementName=PART_ElemTags, Path=Visibility}" Value="Visible">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <StackPanel>
                                                            <StackPanel x:Name="PART_ElemCategories" Margin="0,0,0,20">
                                                                <!-- FontSize="{DynamicResource FontSizeLarger}" -->
                                                                <Label 
                                                                           Foreground="{DynamicResource TextBrush}"
                                                                           FontWeight="Medium"
                                                                           Content="{DynamicResource LOCGameCategoriesTitle}" />
                                                                <Grid MinHeight="45">
                                                                    <ItemsControl ItemsSource="{Binding Game.Categories}" Margin="0,0,0,3">
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate DataType="{x:Type Link}">
                                                                                <Button Content="{Binding Name}"
                                                                                            Style="{StaticResource PropertyItemRoundedPanelButton}"
                                                                                            Command="{Binding DataContext.SetCategoryFilterCommand, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}"
                                                                                            CommandParameter="{Binding}"/>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>
                                                                </Grid>
                                                            </StackPanel>

                                                            <StackPanel x:Name="PART_ElemFeatures">
                                                                <Label Style="{DynamicResource PanelLabel}"
                                                                       Content="{DynamicResource LOCFeaturesLabel}" />
                                                                <Grid Margin="0,0,0,10" MinHeight="45">
                                                                    <ItemsControl ItemsSource="{Binding Game.Features}" Margin="0,0,0,3">
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate>
                                                                                <Button Content="{Binding Name}" Style="{StaticResource PropertyItemRoundedPanelButton}"
                                                                                Command="{Binding DataContext.SetFeatureFilterCommand, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}"
                                                                                CommandParameter="{Binding}"/>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>
                                                                </Grid>
                                                            </StackPanel>
                                                            <StackPanel x:Name="PART_ElemGenres" Margin="0,0,0,20">
                                                                <!-- FontSize="{DynamicResource FontSizeLarger}" -->
                                                                <Label 
                                                                           Foreground="{DynamicResource TextBrush}"
                                                                           FontWeight="Medium"
                                                                           Content="{DynamicResource LOCGenreLabel}" />
                                                                <Grid MinHeight="45">
                                                                    <ItemsControl ItemsSource="{Binding Game.Genres}" Margin="0,0,0,3">
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate>
                                                                                <Button Content="{Binding Name}" Style="{StaticResource PropertyItemRoundedPanelButton}"
                                                                                Command="{Binding DataContext.SetGenreFilterCommand, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}"
                                                                                CommandParameter="{Binding}"/>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>
                                                                </Grid>
                                                            </StackPanel>

                                                            <StackPanel x:Name="PART_ElemTags">
                                                                <Label Style="{DynamicResource PanelLabel}"
                                                                       Content="{DynamicResource LOCTagsLabel}" />
                                                                <Grid Margin="0,0,0,10" MinHeight="45">
                                                                    <ItemsControl ItemsSource="{Binding Game.Tags}" Margin="0,0,0,3">
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate>
                                                                                <Button Content="{Binding Name}" Style="{StaticResource PropertyItemRoundedPanelButton}"
                                                                                Command="{Binding DataContext.SetTagFilterCommand, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}"
                                                                                CommandParameter="{Binding}"/>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>
                                                                </Grid>
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Border>
                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{PluginSettings Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, Plugin=SuccessStory, Path=HasData}">
                                                        <StackPanel Margin="0,15,0,0">
                                                            <Label Style="{DynamicResource PanelLabel}"
                                                                   Content="{DynamicResource LOCSuccessStoryAchievements}" />
                                                            <ContentControl x:Name="SuccessStory_PluginProgressBar" />
                                                            <ContentControl x:Name="SuccessStory_PluginCompactUnlocked" Margin="0,10,20,0" />
                                                            <ContentControl x:Name="SuccessStory_PluginCompactLocked" Margin="0,10,20,0" />
                                                            <ContentControl x:Name="SuccessStory_PluginUserStats" Margin="0,10,20,0" />
                                                            <ContentControl x:Name="SuccessStory_PluginList" Margin="0,10,20,0" MinHeight="550" />
                                                            <ContentControl x:Name="SuccessStory_PluginChart" Margin="0,10,20,0" />
                                                        </StackPanel>
                                                    </Border>

                                                    <Border Visibility="{PluginSettings Plugin=VNDBNexus, Path=IsControlVisible, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                            Style="{DynamicResource DetailsPanelRoundedBorder}">
                                                        <StackPanel Margin="0,15,0,0">
                                                            <Label Style="{DynamicResource PanelLabel}"
                                                                   Content="VNDB" />
                                                            <ContentControl x:Name="VNDBNexus_VndbVisualNovelViewControl" Margin="0,5,0,5" FontWeight="Normal"/>
                                                        </StackPanel>
                                                    </Border>

                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{PluginSettings Plugin=GameActivity, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <StackPanel>
                                                            <Label Style="{DynamicResource PanelLabel}"
                                                                   Content="{DynamicResource LOCGameActivityTitle}" />
                                                            <ContentControl x:Name="GameActivity_PluginChartTime" Margin="0,10,0,0" />
                                                        </StackPanel>
                                                    </Border>

                                                    <Border Style="{DynamicResource DetailsPanelRoundedBorder}"
                                                            Visibility="{PluginSettings Plugin=CheckDlc, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <StackPanel>
                                                            <StackPanel.Resources>
                                                                <Style TargetType="ListBox">
                                                                    <Setter Property="Background" Value="Transparent" />
                                                                    <Setter Property="BorderThickness" Value="0" />
                                                                </Style>
                                                            </StackPanel.Resources>
                                                            <Label Style="{DynamicResource PanelLabel}"
                                                                   Content="DLC" />
                                                            <ContentControl x:Name="CheckDlc_PluginListDlcAll" Height="700" Margin="0,5,0,5"/>
                                                            <ContentControl x:Name="CheckDlc_PluginListDlcOwned" Height="700" Margin="0,5,0,5"/>
                                                            <ContentControl x:Name="CheckDlc_PluginListDlcNotOwned" Height="700" Margin="0,5,0,5"/>
                                                        </StackPanel>
                                                    </Border>

                                                </StackPanel>
                                            </Grid>
                                        </TabItem>
                                        <!--#region WebExplorer-->
                                        <TabItem Header="{DynamicResource LOCWebExplorer_WebLabel}"
                                                 Visibility="{Binding ElementName=WebExplorer_BrowserHostViewControl, Path=Content.IsControlVisible, Converter={StaticResource BooleanToVisibilityConverter}, TargetNullValue=Collapsed, FallbackValue=Collapsed}">
                                            <Grid Margin="0,15,0,50">
                                                <Border Style="{DynamicResource DetailsPanelRoundedBorder}" />
                                                <ContentControl x:Name="WebExplorer_BrowserHostViewControl"
                                                                Height="800"
                                                                Margin="0,5,0,5" />
                                            </Grid>
                                        </TabItem>
                                        <!--#endregion-->

                                    </TabControl>
                                </Grid>

                            </Grid>
                        </Grid>
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>